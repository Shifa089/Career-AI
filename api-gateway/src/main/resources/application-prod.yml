spring:
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
      password: ${REDIS_PASSWORD}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL}

management:
  tracing:
    sampling:
      probability: 0.1

logging:
  level:
    org.springframework.cloud.gateway: INFO
