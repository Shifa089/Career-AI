apiVersion: v1
kind: Service
metadata:
  name: interview-service
  namespace: careerai
  labels:
    app: interview-service
    app.kubernetes.io/part-of: careerai
spec:
  type: ClusterIP
  selector:
    app: interview-service
  ports:
    - name: http
      port: 8083
      targetPort: 8083
