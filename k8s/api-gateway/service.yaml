apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: careerai
  labels:
    app: api-gateway
    app.kubernetes.io/part-of: careerai
spec:
  type: LoadBalancer
  selector:
    app: api-gateway
  ports:
    - name: http
      port: 80
      targetPort: 8080
