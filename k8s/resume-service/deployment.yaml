apiVersion: apps/v1
kind: Deployment
metadata:
  name: resume-service
  namespace: careerai
  labels:
    app: resume-service
    app.kubernetes.io/part-of: careerai
spec:
  replicas: 2
  selector:
    matchLabels:
      app: resume-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: resume-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: /actuator/prometheus
        prometheus.io/port: "8082"
    spec:
      terminationGracePeriodSeconds: 45
      containers:
        - name: resume-service
          image: ghcr.io/shifa089/careerai/resume-service:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8082
          envFrom:
            - configMapRef:
                name: careerai-config
            - secretRef:
                name: careerai-secrets
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 60
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
      imagePullSecrets:
        - name: ghcr-pull
