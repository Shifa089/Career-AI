apiVersion: v1
kind: Service
metadata:
  name: resume-service
  namespace: careerai
  labels:
    app: resume-service
    app.kubernetes.io/part-of: careerai
spec:
  type: ClusterIP
  selector:
    app: resume-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082
