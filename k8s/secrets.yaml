# ---------------------------------------------------------------------------
# TEMPLATE ONLY — do NOT commit real secret values.
#
# In a real deployment these values should be injected by the CI pipeline or,
# preferably, sourced from Kubernetes External Secrets backed by AWS Secrets
# Manager or HashiCorp Vault. The placeholders below are intentionally empty.
# ---------------------------------------------------------------------------
apiVersion: v1
kind: Secret
metadata:
  name: careerai-secrets
  namespace: careerai
  labels:
    app.kubernetes.io/part-of: careerai
type: Opaque
stringData:
  DB_PASSWORD: "CHANGE_ME"
  REDIS_PASSWORD: "CHANGE_ME"
  JWT_SECRET: "CHANGE_ME_AT_LEAST_256_BITS_LONG"
  ANTHROPIC_API_KEY: "CHANGE_ME"
  OPENAI_API_KEY: "CHANGE_ME"
  GOOGLE_CLIENT_ID: "CHANGE_ME"
  GOOGLE_CLIENT_SECRET: "CHANGE_ME"
  GITHUB_CLIENT_ID: "CHANGE_ME"
  GITHUB_CLIENT_SECRET: "CHANGE_ME"
  AWS_ACCESS_KEY_ID: "CHANGE_ME"
  AWS_SECRET_ACCESS_KEY: "CHANGE_ME"
  ADZUNA_APP_ID: ""
  ADZUNA_APP_KEY: ""
