apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: careerai
  labels:
    app: auth-service
    app.kubernetes.io/part-of: careerai
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - name: http
      port: 8081
      targetPort: 8081
