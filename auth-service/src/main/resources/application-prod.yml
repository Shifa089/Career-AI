spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/auth_db
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    show-sql: false

management:
  tracing:
    sampling:
      probability: 0.1

logging:
  level:
    com.careerai.auth: INFO
