spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/jobmatch_db
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

management:
  tracing:
    sampling:
      probability: 0.1

logging:
  level:
    com.careerai.jobmatch: INFO
