spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/jobmatch_db
    username: careerai
    password: careerai
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.careerai.jobmatch: DEBUG
    org.hibernate.SQL: DEBUG
