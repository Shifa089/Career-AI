spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/interview_db
    username: careerai
    password: careerai
  jpa:
    show-sql: true

logging:
  level:
    com.careerai.interview: DEBUG
